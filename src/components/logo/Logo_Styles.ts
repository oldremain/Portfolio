import styled from "styled-components";

const Logo = styled.a`
  display: flex;
  align-items: center;
  gap: 8px;
`;

const Name = styled.div`
  font-weight: 700;
  font-size: 20px;
  color: #fff;
`;

export const S = {
  Logo,
  Name,
};
